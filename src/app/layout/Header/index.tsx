const Header = () => {
  return (
    <header className="fixed inset-x-0 inset-s-0 py-5">
      <nav className="container flex items-center justify-between">Header</nav>
    </header>
  );
};

export default Header;
